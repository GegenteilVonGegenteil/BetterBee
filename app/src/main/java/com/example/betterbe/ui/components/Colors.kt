package com.example.betterbe.ui.components

import androidx.annotation.ColorRes
import com.example.betterbe.R

enum class Colors(@ColorRes val colorResId: Int) {
        Red (R.color.red_light),
        Orange (R.color.orange_light),
        Yellow (R.color.yellow_light),
        Green (R.color.green_light),
        Blue (R.color.blue_light),
        Violet (R.color.violet_light)
}